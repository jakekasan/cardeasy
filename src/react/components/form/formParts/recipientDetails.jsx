import React from "react";

export const RecipientDetails = ({
    onChange,
    values,
    stepNumber
}) => {
    return (
        <section>
            <h5>Step {stepNumber}: Recipient Details</h5>
        </section>
    )
}