export const WriteCardMessage = ({
    values,
    onChange
}) => {
    return (
        <section>
            <h5>Step {stepNumber}: Write your message</h5>
            <textarea></textarea>
        </section>
    )
}